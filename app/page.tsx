import styles from "./HomePage.module.scss";

const Page = () => {
  return (
    <h1
      style={{
        color: styles.primaryColor,
      }}
    >
      Hello, Next.js!
    </h1>
  );
};

export default Page;
